package PGO3.Lecture;
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        new Employee(scanner.next(), scanner.next(), scanner.nextInt(), scanner.nextInt(), scanner.next());
    }
}
